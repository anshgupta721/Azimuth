

#[derive(Debug, Clone)]
pub struct HarmonicCoeffs {
    degree: usize,
    c: Vec<Vec<f64>>,
    s: Vec<Vec<f64>>,
}

// impl HarmonicCoeffs {
//     pub fn zeros(degree: usize) -> Self {
//         let c = // list of 
//         let s = 

//     }
// }

